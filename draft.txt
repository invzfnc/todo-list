clear on each command Add task

cout << "Enter task details   : " << endl
     << "Title                : " << title << endl
     << "Description          : " << description << endl
     << "Due Date (YYYY-MM-DD): " << time << endl << endl
     << "Task added successfully. ";

View tasks

cout << "1  Title      : " << title << endl
     << "   Description: " << description << endl
     << "   Due Date   : " << time << endl

TODO (if else):
validate date
- Day: 30 or 31, 28 or 29
- Month: in range 1-12
- Year: four digits
___________________________________________

***Due date

#include <iostream>
using namespace std;

int main() {
    int year;
    int month;
    int day;
    
    cout << "Year: ";
    cin >> year;

	if (year <= 999)
	{
		cout << "Invalid. Please enter a valid year. \nYear: ";
		cin >> year;
	}
	
    
    while ( year >= 1000 ) {

	    cout << "Month: ";
	    cin >> month;

		while ( month <=0 && month >= 13) {

			cout << "Invalid month, please enter again: ";
			cin >> month;

		}
	    
		while ( month == 1 || month == 3 || month == 5 || month == 7 || month == 8 || month == 10 || month == 12 ) {

		    cout << "Day: ";
		    cin >> day;
		    
			if ( day >= 1 && day <= 31)
			{
		    	cout << "Due date: " << day << "/" << month << "/" << year;
				
			} else if ( day < 1 && day > 31) {
			
			cout << "Invalid. Please enter a valid day: ";
			cin >> day; 

			}
			
		} 
		
		while ( month == 4 || month == 6 || month == 9 || month == 11 ) {

		    cout << "Day: ";
		    cin >> day;
		    
			if ( day >= 1 && day <= 30)
			{
		    	cout << "Due date: " << day << "/" << month << "/" << year;
				
			} else if ( day < 1 && day > 30) {
			
			cout << "Invalid. Please enter a valid day: "; 
			cin >> day;

			}
			
	    } 
		
		while ( month == 2 ) {

		    cout << "Day: ";
		    cin >> day;
		    
			if ( day >= 1 && day <= 29)
			{
		    	cout << "Due date: " << day << "/" << month << "/" << year;
				
			} else if ( day < 1 && day > 29) {
			
			cout << "Invalid. Please enter a valid day: ";
			cin >> day;  

			}

	    }
		
	} 
	
}